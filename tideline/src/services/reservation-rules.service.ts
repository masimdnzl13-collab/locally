import type { Db } from '../db/database.js';
export class ReservationRulesService { constructor(private db: Db) {} get(restaurantId: string) { return this.db.prepare('SELECT * FROM reservation_settings WHERE restaurant_id=?').get(restaurantId) ?? { state: 'NOT_CONFIGURED' }; } canAcceptParty(restaurantId: string, size: number) { const s=this.get(restaurantId) as any; if ('state' in s) return { allowed:false, reason:'NOT_CONFIGURED' }; if (!s.reservations_enabled) return {allowed:false,reason:'DISABLED'}; if (size<s.minimum_party_size||size>s.maximum_party_size) return {allowed:false,reason:'PARTY_SIZE'}; if (size>=s.large_party_threshold&&s.large_party_requires_human) return {allowed:false,reason:'HUMAN_REQUIRED'}; return {allowed:true}; } }
