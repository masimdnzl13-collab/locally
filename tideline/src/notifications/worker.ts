import { Worker } from 'bullmq'; import type { Db } from '../db/database.js'; import { NotificationService } from './notification.service.js'; import type { MessagingProvider } from './contracts.js'; import { notificationQueueName } from './queue.js';
export function startNotificationWorker(db:Db,provider:MessagingProvider){const url=new URL(process.env.REDIS_URL??'redis://localhost:6379');return new Worker(notificationQueueName,async job=>new NotificationService(db,provider).process(job.data.notificationId),{connection:{host:url.hostname,port:Number(url.port||6379),password:url.password||undefined},concurrency:Number(process.env.NOTIFICATION_WORKER_CONCURRENCY??5)})}
