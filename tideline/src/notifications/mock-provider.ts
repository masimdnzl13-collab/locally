import type { MessagingProvider } from './contracts.js'; export class MockMessagingProvider implements MessagingProvider { readonly sent:{to:string;from:string;body:string;providerMessageId:string}[]=[]; async sendMessage(i:{to:string;from:string;body:string}){const providerMessageId=`mock-${this.sent.length+1}`;this.sent.push({...i,providerMessageId});return{providerMessageId,providerStatus:'sent'}} async getMessageStatus(){return{status:'sent'}} }
